编译方法：
cd EnhancedLiDAR

catkin_make

source devel/setup.bash

roslaunch EnhancedLiDAR EnhancedLiDAR.launch

//rosbag play --clock test.bag

rosrun rviz rviz

配置Rviz的Frame为velodyne（失败尝试base_link），并且add原始点云和过滤以后的点云的display



GitHub提交方法：
cd EnhancedLiDAR

git add .      				//将要提交的文件暂存到本地仓库。

git commit -m "Add existing file"     	//提交暂存在本地仓库中的文件。

(proxychains) git push origin master	//推送更改（本地仓库中）到 GitHub。
